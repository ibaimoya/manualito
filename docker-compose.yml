services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True
    volumes:
      - ./backend/.paddleocr_cache:/root/.paddlex
