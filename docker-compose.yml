name: ${PROYECT_NAME:-manualito}

services:

  python-base:
    build:
      context: .
      dockerfile: backend/python-base/Dockerfile
      args:
        UV_VERSION: ${UV_VERSION:?UV_VERSION no definida}
        PYTHON_VERSION: ${PYTHON_VERSION:?PYTHON_VERSION no definida}
    scale: 0
    network_mode: none

  api:
    container_name: ${PROYECT_NAME:-manualito}-api
    env_file:
      - ./config/backend.env
    build:
      context: .
      dockerfile: backend/api/Dockerfile
      additional_contexts:
        - "python-base:${PYTHON_BASE_TAG:-local}=service:python-base"
      args:
        PYTHON_BASE_TAG: ${PYTHON_BASE_TAG:-local}
    ports:
      - "8000:8000"
    depends_on:
      ocr:
        condition: service_healthy
      rag:
        condition: service_healthy
      llm:
        condition: service_healthy
    networks:
      - manualito-net

  ocr:
    container_name: ${PROYECT_NAME:-manualito}-ocr
    env_file:
      - ./config/backend.env
    image: ${PROYECT_NAME:-manualito}-ocr:cpu
    build:
      context: .
      dockerfile: backend/ocr/Dockerfile
      additional_contexts:
        - "python-base:${PYTHON_BASE_TAG:-local}=service:python-base"
      args:
        OCR_RUNTIME_STAGE: ${OCR_RUNTIME_STAGE:-tesseract}
        PYTHON_BASE_TAG: ${PYTHON_BASE_TAG:-local}
    environment:
      OCR_ENGINE: ${OCR_RUNTIME_STAGE:-tesseract}
    networks:
      - manualito-net

  rag:
    container_name: ${PROYECT_NAME:-manualito}-rag
    env_file:
      - ./config/backend.env
    build:
      context: .
      dockerfile: backend/rag/Dockerfile
      additional_contexts:
        - "python-base:${PYTHON_BASE_TAG:-local}=service:python-base"
      args:
        PYTHON_BASE_TAG: ${PYTHON_BASE_TAG:-local}
    volumes:
      - ./backend/rag/.hf_cache:/root/.cache/huggingface
    depends_on:
      - chroma
    networks:
      - manualito-net

  chroma:
    container_name: ${PROYECT_NAME:-manualito}-chroma
    image: chromadb/chroma:${CHROMA_VERSION:-1.5.7}
    volumes:
      - chromadb-data:/chroma/chroma
    networks:
      - manualito-net

  llm:
    container_name: ${PROYECT_NAME:-manualito}-llm
    env_file:
      - ./config/backend.env
    build:
      context: .
      dockerfile: backend/llm/Dockerfile
      additional_contexts:
        - "python-base:${PYTHON_BASE_TAG:-local}=service:python-base"
      args:
        PYTHON_BASE_TAG: ${PYTHON_BASE_TAG:-local}
    depends_on:
      ollama-init:
        condition: service_completed_successfully
    networks:
      - manualito-net

  ollama:
    container_name: ${PROYECT_NAME:-manualito}-ollama
    image: ollama/ollama:${OLLAMA_VERSION:-0.23.1}
    volumes:
      - ollama-data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 60s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - manualito-net

  # Descarga el modelo si falta.
  ollama-init:
    container_name: ${PROYECT_NAME:-manualito}-ollama-init
    env_file:
      - ./config/backend.env
    image: ollama/ollama:${OLLAMA_VERSION:-0.23.1}
    depends_on:
      ollama:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command: ["ollama pull $$OLLAMA_MODEL"]
    restart: "no"
    networks:
      - manualito-net

networks:
  manualito-net:
    driver: bridge

volumes:
  chromadb-data:
  ollama-data:
