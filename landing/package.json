{
  "name": "manualito-landing",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=26.9.0"
  },
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 5228",
    "check": "html-validate public",
    "test": "playwright test",
    "verify": "pnpm check && pnpm test && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "html-validate": "11.16.0",
    "wrangler": "4.135.0"
  }
}
